/* Вставьте в Tawk.to: Administration → Chat Widget → Widget Appearance → Advanced → Custom CSS */

/* Кнопка чата — красный СКМ */
.tawk-button,
.tawk-button-circle,
.tawk-button-large {
  background-color: #e30613 !important;
  border-color: #e30613 !important;
}

/* Шапка виджета */
.tawk-header,
.tawk-card-header {
  background-color: #e30613 !important;
  color: #ffffff !important;
}

/* Фон окна чата */
.tawk-chat-panel,
.tawk-card {
  background-color: #18181b !important;
  color: #ffffff !important;
}

/* Сообщения оператора */
.tawk-message-agent .tawk-message-body {
  background-color: #e30613 !important;
  color: #ffffff !important;
}

/* Сообщения посетителя */
.tawk-message-visitor .tawk-message-body {
  background-color: #27272a !important;
  color: #ffffff !important;
}